eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka
spring:
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true #允许从注册表拉取配置地址
      name: user-service
      profile: dev